# Crisp File Explorer 维护说明

当前版本：v0.2.50

作者：letschips（小红书博主）

## 当前实现

- 从 Obsidian 文件浏览器中的文件和文件夹标题生成纵向刻度轨道。
- 使用弹簧动画在活动项目之间移动 orb。
- 支持拖动导航、边缘滚动、常用文件磁吸、文件夹停留展开和今日轨迹。
- 支持内置 SVG、人物图片、每日随机样式及可匹配 orb 的音效。
- 动画帧只更新 orb 附近的刻度与标题，普通滚动不会重新测量整棵文件树。
- 每个文件浏览器面板拥有独立控制器，并支持 Obsidian 独立窗口中的文件浏览器。

## v0.2.41 – v0.2.43 维护范围

- 授权校验升级：移除旧公钥，仅接受新版授权码；存量授权码需重新签发。
- 在线设备校验改用 Obsidian `requestUrl`，修复 Electron/CSP 环境下校验失败的问题。
- 未修改弹簧、目标位置、拖动、旋转、音效或视觉参数。

## v0.2.39 维护范围

- Activity / Heatmap 仅增量更新 `is-today` 与 `is-magnet`，不再覆盖动态横线 class。
- 文件树结构未变化时保留当前刻度 transform、动态范围和最近刻度，避免点击切换时出现一帧空白。
- 文件树结构变化时完整重置动态刻度并在同一刷新任务内立即绘制，既避免残留，也不露出空白帧。
- 未修改弹簧、目标位置、拖动、旋转、音效或视觉参数。

## v0.2.37 维护范围

- 删除活动文件与活动文件夹的 `::before` 竖线及其动画分支。
- 保留横向轨道、端点圆点、活动标题背景与主题色适配。
- 未修改弹簧、目标位置、拖动、旋转或音效参数。

## v0.2.36 维护范围

- 在插件文件浏览器容器内统一活动项背景、强调线、圆点和过渡，抵抗普通主题与 CSS snippet 的活动状态覆盖。
- 活动标题使用不参与布局的外扩阴影，不再通过 padding 增高文件行。
- 插件颜色继续读取 Obsidian 运行时强调色；主题仍负责字体、图标、基础行高与文件树间距。
- 未修改弹簧、目标位置、拖动、旋转或音效参数。

## v0.2.35 维护范围

- 文件与文件夹重命名时迁移今日轨迹和常用文件统计，删除时移除失效路径。
- 独立窗口中的拖动释放事件由目标文件树的 `defaultView` 创建。
- 保留 v0.2.34 的多窗口控制器、资源清理和视觉手感。

## v0.2.34 维护范围

- 节点创建、动画帧、拖动监听、计时器和可见性判断绑定到文件浏览器所在的 `ownerDocument` 与 `defaultView`。
- 从所有 `file-explorer` leaves 收集容器，同时保留主工作区 DOM 扫描作为兼容回退。
- 每个承载文件浏览器的文档独立添加插件作用域 class，卸载时统一清理。
- 删除已被独立 SVG 文件替代的 Soccer、Basketball、Tennis 内联图形。
- 删除未引用的旧素材和系统元数据文件。

## 有意保持不变

- 弹簧参数、动画曲线和目标位置计算。
- 拖动、边缘滚动、磁吸和文件夹自动展开手感。
- orb 尺寸、旋转方式、阴影、颜色及轨道视觉。
- 音效映射和用户现有设置。
- 不提供 orb 键盘导航，避免再次拦截 Obsidian 全局快捷键。

## 维护验证

在插件目录执行：

```bash
node --check main.js
node --test tests/regression.test.js
```

发布前还需要在 Obsidian 中重载插件，确认主窗口和独立窗口的文件浏览器均能显示、拖动和清理 orb，并检查开发者错误日志。

## 文件职责

- `main.js`：插件运行逻辑。
- `styles.css`：轨道、orb、设置页及状态样式。
- `manifest.json`：插件身份与版本。
- `assets/`：运行时引用的图片和 SVG。
- `data.json`：当前 vault 的用户设置与活动记录，不应放入分享包。
- `tests/regression.test.js`：自动回归测试。
- `CHANGELOG.md`：版本变更记录。

灵感来源：[BubblePtr/ZenBlog CrispToc](https://github.com/BubblePtr/ZenBlog/blob/main/src/shared/components/navigation/CrispToc.client.tsx)
